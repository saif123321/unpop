<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /new-demo/

  # Don't rewrite real files or directories
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # SPA fallback — React Router
  RewriteRule ^ index.html [L]
</IfModule>
